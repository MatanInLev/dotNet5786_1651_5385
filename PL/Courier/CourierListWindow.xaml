<Window x:Class="PL.Courier.CourierListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Courier"
        mc:Ignorable="d"
        Title="CourierListWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <!--External Grid-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40*"/>
                <RowDefinition Height="500*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!--Internal, Upper Grid-->
            <Grid HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Label" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0"/>
                <ComboBox
                    ItemsSource="{Binding Source={StaticResource ActiveFilterCollectionKey}}"
                    SelectedValue="{Binding Filter, Mode=TwoWay}"
                    SelectionChanged="FilterComboBox_SelectionChanged"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" />

            </Grid>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Path=CourierList}" SelectedItem="{Binding SelectedCourier}" MouseDoubleClick="courierList_MouseDoubleClick"/>
            <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Height="50" Width="100" Click="addCourierButton_Click"/>
        </Grid>
        
    </Grid>
</Window>

<Window x:Class="PL.Courier.CourierMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Courier - Main Screen"
        Width="900"
        Height="620"
        WindowStartupLocation="CenterScreen"
        Background="White"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Border CornerRadius="14"
                BorderBrush="Black"
                BorderThickness="2"
                Background="White"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Courier screen - main"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="Black"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>

                <GroupBox Grid.Row="1" Header="Courier details" Margin="0,0,0,12">
                    <Grid Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Id:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding CurrentCourier.Id}" Grid.Column="1"/>
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="2" Header="Current order in handling (only if exists)" Margin="0,0,0,12">
                    <StackPanel Margin="8">
                        <TextBlock Text="Order details go here"/>
                    </StackPanel>
                </GroupBox>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                    <Button Content="Delivery history" Width="200" Height="40" Margin="10,0" Click="BtnHistory_Click"/>
                    <Button Content="Order selection" Width="200" Height="40" Margin="10,0" Click="BtnOrderSelection_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>